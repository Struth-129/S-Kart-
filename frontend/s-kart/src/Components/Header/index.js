import React, { Component } from 'react';
import './style.css';
const Header = (props)=> {
        return (
            <div className="header">
                Header
            </div>
        );
}

export default Header;